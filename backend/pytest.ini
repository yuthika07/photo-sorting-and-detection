[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: exercises a real bundled ML model (SCRFD/ArcFace) end to end, not mocked. Skip with -m "not slow" for a fast run.
addopts = -ra
